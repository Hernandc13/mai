{{!
    @template local_mai/dashboard

    Dashboard principal del plugin MAI.
}}

<style>
.local-mai-dashboard-wrapper {
    max-width: 980px;
    margin: 0 auto;
    padding: 8px 0 24px;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

/* Paleta MAI */
.local-mai-dashboard-wrapper {
    --mai-maroon: #8C253E;
    --mai-orange: #FF7000;
    --mai-bg-soft: #f8fafc;
    --mai-border-soft: #e5e7eb;
    --mai-text-main: #111827;
    --mai-text-muted: #6b7280;
}

/* Encabezado */
.local-mai-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin: 10px 0 18px;
    gap: 12px;
}

.local-mai-header-left {
    max-width: 70%;
}

.local-mai-header-title {
    font-weight: 700;
    font-size: 1.25rem;
    color: var(--mai-maroon);
    margin-bottom: 3px;
}

.local-mai-header-subtitle {
    font-size: 0.86rem;
    color: var(--mai-text-muted);
}

.local-mai-header-tag {
    padding: 4px 12px;
    border-radius: 999px;
    background: linear-gradient(135deg, var(--mai-maroon), var(--mai-orange));
    color: #ffffff;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .09em;
    white-space: nowrap;
}

/* Grid de módulos */
.local-mai-modules {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 18px;
}

/* Card */
.local-mai-module-card {
    position: relative;
    border-radius: 16px;
    padding: 14px 16px 12px;
    background: #ffffff;
    border: 1px solid var(--mai-border-soft);
    box-shadow: 0 10px 20px rgba(15, 23, 42, 0.06);
    cursor: pointer;
    text-decoration: none;
    color: inherit;
    transition:
        transform .15s ease,
        box-shadow .15s ease,
        border-color .15s ease,
        background .15s ease;
}

.local-mai-module-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 16px 30px rgba(15, 23, 42, 0.10);
    border-color: rgba(140, 37, 62, 0.25);
    background: var(--mai-bg-soft);
}

/* Contenido: icono + badge */
.local-mai-module-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
}

.local-mai-module-icon {
    width: 42px;
    height: 42px;
    border-radius: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    background: rgba(140, 37, 62, 0.04);
    color: var(--mai-maroon);
}

.local-mai-module-badge {
    padding: 3px 10px;
    border-radius: 999px;
    background: rgba(255, 112, 0, 0.08);
    color: var(--mai-orange);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .08em;
}

/* Título y descripción (por si se amplía en el futuro) */
.local-mai-module-title {
    margin: 0;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--mai-text-main);
}

/* Pie: link */
.local-mai-module-footer {
    margin-top: 8px;
    display: flex;
    justify-content: flex-end;
}

.local-mai-module-link {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-size: 0.78rem;
    color: var(--mai-maroon);
    font-weight: 500;
}

.local-mai-module-card:hover .local-mai-module-link {
    color: var(--mai-orange);
}

.local-mai-module-link span:last-child {
    font-size: 0.9rem;
}

/* Variaciones por módulo (ligero acento) */
.local-mai-module-participacion .local-mai-module-icon {
    background: rgba(255, 112, 0, 0.06);
    color: var(--mai-orange);
}

.local-mai-module-estructura .local-mai-module-icon {
    background: rgba(140, 37, 62, 0.06);
    color: var(--mai-maroon);
}

.local-mai-module-panel .local-mai-module-icon {
    background: linear-gradient(
        135deg,
        rgba(140, 37, 62, 0.10),
        rgba(255, 112, 0, 0.10)
    );
    color: var(--mai-maroon);
}

/* Responsive */
@media (max-width: 600px) {
    .local-mai-header {
        flex-direction: column;
        align-items: flex-start;
    }
    .local-mai-header-left {
        max-width: 100%;
    }
}
</style>

<div class="local-mai-dashboard-wrapper">

    <div class="local-mai-header">
        <div class="local-mai-header-left">
            <div class="local-mai-header-title">
                Monitoreo Académico Integral
            </div>
            <div class="local-mai-header-subtitle">
                Accede rápidamente a los módulos clave de seguimiento académico MAI.
            </div>
        </div>
        <div class="local-mai-header-tag">
            Dashboard MAI
        </div>
    </div>

    <div class="local-mai-modules">

        <!-- Seguimiento de participación -->
        <a href="./participacion.php" class="local-mai-module-card local-mai-module-participacion">
            <div class="local-mai-module-top">
                <div class="local-mai-module-icon">
                    <i class="fa fa-users" aria-hidden="true"></i>
                </div>
                <span class="local-mai-module-badge">Participación</span>
            </div>
            <h3 class="local-mai-module-title">Seguimiento de participación</h3>
            <div class="local-mai-module-footer">
                <span class="local-mai-module-link">
                    Ver módulo
                    <span>➜</span>
                </span>
            </div>
        </a>

        <!-- Estructura académica -->
        <a href="./estructura.php" class="local-mai-module-card local-mai-module-estructura">
            <div class="local-mai-module-top">
                <div class="local-mai-module-icon">
                    <i class="fa fa-sitemap" aria-hidden="true"></i>
                </div>
                <span class="local-mai-module-badge">Estructura</span>
            </div>
            <h3 class="local-mai-module-title">Estructura académica</h3>
            <div class="local-mai-module-footer">
                <span class="local-mai-module-link">
                    Ver módulo
                    <span>➜</span>
                </span>
            </div>
        </a>

        <!-- Panel de control -->
        <a href="./panel/index.php" class="local-mai-module-card local-mai-module-panel">
            <div class="local-mai-module-top">
                <div class="local-mai-module-icon">
                    <i class="fa fa-chart-line" aria-hidden="true"></i>
                </div>
                <span class="local-mai-module-badge">Panel</span>
            </div>
            <h3 class="local-mai-module-title">Panel de control MAI</h3>
            <div class="local-mai-module-footer">
                <span class="local-mai-module-link">
                    Abrir panel
                    <span>➜</span>
                </span>
            </div>
        </a>

    </div>
</div>
